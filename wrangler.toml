name = "lindsaydesignco"
compatibility_date = "2024-01-01"

# Cloudflare Pages configuration
pages_build_output_dir = "."

# Optional: Add environment variables here if needed in the future
# [vars]
# API_KEY = "your-api-key"

# Optional: Add KV namespaces, D1 databases, or R2 buckets here if needed
# [[kv_namespaces]]
# binding = "MY_KV"
# id = "your-kv-id"
